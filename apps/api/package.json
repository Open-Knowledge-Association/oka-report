{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --outfile=dist/oka-api src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/utils": "workspace:*",
    "hono": "^4.11.4",
    "node-cron": "^3.0.3",
    "p-limit": "^7.3.0",
    "puppeteer": "^23.6.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
